{
  "name": "campus-dreamer",
  "version": "1.0.0",
  "description": "校园梦想家 - Campus Dreamer",
  "scripts": {
    "build": "npx cap sync android",
    "android": "npm run build && npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.0.0"
  }
}
